use std::net::{TcpStream, TcpListener};
use std::io::{Read, Write};

fn client_manager(stream : TcpStream) {

    

    let mut stream = TcpStream::Conect("127.0.0.1:8080")
     .expect("No se ha podido conectar al servidor");

        
}